name: GitHub Release

on:
  push:
    tags: ["v*.*.*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Verify version
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          python - <<'PY'
          import os, tomllib
          from aec_link_version import VERSION
          tag = os.environ["RELEASE_TAG"].removeprefix("v")
          project = tomllib.load(open("pyproject.toml", "rb"))["project"]["version"]
          if tag != VERSION or tag != project:
              raise SystemExit(f"version mismatch: tag={tag}, runtime={VERSION}, project={project}")
          PY
      - name: Package extension
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: zip -r "arcenciel-link-comfyui-${RELEASE_TAG}.zip" . -x ".git/*" ".github/*" "tests/*" "*/__pycache__/*" "*.pyc" ".ruff_cache/*" ".pytest_cache/*"
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.ref_name }}
        run: gh release create "$RELEASE_TAG" "arcenciel-link-comfyui-${RELEASE_TAG}.zip" --verify-tag --generate-notes --title "ArcEnCiel Link ComfyUI ${RELEASE_TAG}"
